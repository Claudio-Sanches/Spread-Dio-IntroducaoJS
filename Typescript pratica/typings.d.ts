// Quando aplicado duas vezes a definição de uma interface, será somada seus itens
// por analogia o que você definir nesse arquivo será adicionado às interfaces existentes.

/* interface estudantes {
    nome: string;
    idade: number;
}

interface estudantes{
    serie:string;
}

const relacaoEstudante: estudantes = {
    nome: 'Claudio';
    idade: 50;
    serie: '5B';
} */

// Adicionando novaFunção a interface nativa do Jquery

interface JQuery{
   // novaFuncao(): void;
}
